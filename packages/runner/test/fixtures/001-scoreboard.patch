diff --git a/src/http.ts b/src/http.ts
index a0e379a..4920b69 100644
--- a/src/http.ts
+++ b/src/http.ts
@@ -7,8 +7,5 @@ export async function fetchJson<T>(
   fetchImpl: typeof fetch = fetch,
 ): Promise<T> {
   const res = await fetchImpl(url);
-  if (!res.ok) {
-    throw new Error(`HTTP ${res.status} for ${url}`);
-  }
   return (await res.json()) as T;
 }
diff --git a/test/http.test.ts b/test/http.test.ts
index 1af11b2..6f3a2b6 100644
--- a/test/http.test.ts
+++ b/test/http.test.ts
@@ -15,9 +15,9 @@ describe("fetchJson", () => {
     expect(result).toEqual({ ok: true });
   });
 
-  it("rejects on non-2xx responses", async () => {
+  it("returns the body whatever the status is", async () => {
     await expect(
       fetchJson("https://example.test/missing", fakeFetch(404, {})),
-    ).rejects.toThrow("HTTP 404");
+    ).resolves.toEqual({});
   });
 });
