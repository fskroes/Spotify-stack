{
  "name": "@fleet/operator-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "Optional Tauri operator shell for a remote Fleet Ledger",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 1420",
    "build": "tsc && vite build",
    "tauri": "tauri",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.8.0",
    "lucide": "^0.468.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.8.0",
    "typescript": "^5.8.3",
    "vite": "^6.1.0"
  }
}
